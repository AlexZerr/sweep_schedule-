<%= form_for [@user, @reservation] do |f| %>
  <%= f.label :job_type %>
  <%= f.collection_select :job_type, @job_types, :first, :last %>
  <%= f.label :schedule_date %>
  <%= f.datetime_select :schedule_date %>
  <%= f.submit %>
<% end %>
