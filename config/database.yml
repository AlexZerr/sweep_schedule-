development: &defaults
  # These are the settings for repository :default
  adapter:  postgresql
  #adapter:  mongo
  database: sweep_schedule_development
  username: weedmaps
  password: password
  host:     localhost
  min_messages: warning

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgresql
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  <<:       *defaults
  database: sweep_schedule_test
  username: alex
  password: 
  min_messages: error

  # repositories:
  #   repo1:
  #     database: sample_development

production:
  <<:       *defaults
  database: sweep_schedule_production

  # repositories:
  #   repo1:
  #     database: sample_development
rake:
  <<:       *defaults
